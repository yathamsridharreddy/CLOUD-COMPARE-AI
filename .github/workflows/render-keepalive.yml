name: Render keep-alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render /health
        run: curl -fsS -o /dev/null -w '%{http_code}\n' "https://cloudcompare-ai-api.onrender.com/health" || true
