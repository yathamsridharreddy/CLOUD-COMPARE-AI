diff --git a/render.yaml b/render.yaml
index 55c09c7..c8a5859 100644
--- a/render.yaml
+++ b/render.yaml
@@ -30,27 +30,18 @@ services:
       - key: CORS_ALLOWED_ORIGINS
         value: https://cloud-compare-ai-flax.vercel.app
       # ── Persistent database (managed Postgres) ─────────────────────────────
-      # Render's free Postgres injects DATABASE_URL (postgres://user:pass@host/db).
-      - key: SPRING_DATASOURCE_URL
+      # Render injects DATABASE_URL as postgresql://user:pass@host:port/db.
+      # RenderDatabaseConfig converts it to a JDBC URL (jdbc:postgresql://...)
+      # with separate user/password, which is what HikariCP requires.
+      - key: DATABASE_URL
         fromDatabase:
           name: cloudcompare-db
           property: connectionString
-      - key: SPRING_DATASOURCE_USERNAME
-        fromDatabase:
-          name: cloudcompare-db
-          property: user
-      - key: SPRING_DATASOURCE_PASSWORD
-        fromDatabase:
-          name: cloudcompare-db
-          property: password
-      - key: SPRING_DATASOURCE_DRIVER_CLASS_NAME
-        value: org.postgresql.Driver
       - key: SPRING_JPA_DATABASE_PLATFORM
         value: org.hibernate.dialect.PostgreSQLDialect
-      - key: SPRING_DATASOURCE_HIKARI_MAX_POOL_SIZE
-        value: "5"
-      - key: SPRING_DATASOURCE_HIKARI_MIN_IDLE
-        value: "1"
+      # update (not create) so registered users survive redeploys.
+      - key: SPRING_JPA_HIBERNATE_DDL_AUTO
+        value: "update"
 
 databases:
   - name: cloudcompare-db
diff --git a/src/main/java/com/cloudcompare/ai/config/RenderDatabaseConfig.java b/src/main/java/com/cloudcompare/ai/config/RenderDatabaseConfig.java
new file mode 100644
index 0000000..9590eaf
--- /dev/null
+++ b/src/main/java/com/cloudcompare/ai/config/RenderDatabaseConfig.java
@@ -0,0 +1,49 @@
+package com.cloudcompare.ai.config;
+
+import com.zaxxer.hikari.HikariDataSource;
+import org.springframework.beans.factory.annotation.Value;
+import org.springframework.boot.autoconfigure.condition.ConditionalOnProperty;
+import org.springframework.context.annotation.Bean;
+import org.springframework.context.annotation.Configuration;
+
+import javax.sql.DataSource;
+import java.net.URI;
+
+/**
+ * Render's managed Postgres exposes a connection string in the libpq form
+ * (postgresql://user:password@host:port/database), which the JDBC driver does
+ * NOT accept. Spring Boot / HikariCP need a JDBC URL
+ * (jdbc:postgresql://host:port/database) with separate username/password.
+ *
+ * This bean is created only when the DATABASE_URL env var is present (i.e. on
+ * Render); locally it is absent, so Spring Boot falls back to the in-memory H2
+ * datasource configured in application.properties.
+ */
+@Configuration
+@ConditionalOnProperty(name = "DATABASE_URL")
+public class RenderDatabaseConfig {
+
+    @Value("${DATABASE_URL}")
+    private String databaseUrl;
+
+    @Bean
+    public DataSource dataSource() {
+        URI uri = URI.create(databaseUrl);
+        String[] userInfo = uri.getUserInfo().split(":", 2);
+        String username = userInfo[0];
+        String password = userInfo.length > 1 ? userInfo[1] : "";
+
+        int port = uri.getPort() == -1 ? 5432 : uri.getPort();
+        String jdbcUrl = "jdbc:postgresql://" + uri.getHost() + ":" + port + uri.getPath();
+
+        HikariDataSource dataSource = new HikariDataSource();
+        dataSource.setJdbcUrl(jdbcUrl);
+        dataSource.setUsername(username);
+        dataSource.setPassword(password);
+        dataSource.setDriverClassName("org.postgresql.Driver");
+        // Sized conservatively to fit Render's free 512 MB web instance.
+        dataSource.setMaximumPoolSize(5);
+        dataSource.setMinimumIdle(1);
+        return dataSource;
+    }
+}
